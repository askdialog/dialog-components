{
  "name": "@askdialog/vue-example",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/vue-example/src",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "pnpm run dev",
        "cwd": "packages/vue-example"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "dependsOn": ["^build"],
      "options": {
        "command": "pnpm run build",
        "cwd": "packages/vue-example"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "pnpm run preview",
        "cwd": "packages/vue-example"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run lint",
        "cwd": "packages/vue-example"
      }
    }
  },
  "tags": ["type:app", "scope:vue"],
  "implicitDependencies": ["@askdialog/dialog-vue", "@askdialog/dialog-sdk"]
}
