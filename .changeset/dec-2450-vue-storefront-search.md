---
"@askdialog/dialog-vue": minor
---

Add the storefront search components: `useDialogSearch` (Vue binding of the SDK search controller) plus `DialogSearchBar`, `DialogSearchResults`, `DialogSearchProductCard` and `DialogSearchPagination`, with DEC-2448 attribution (viewport impressions and select events) wired in.
